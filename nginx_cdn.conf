location /uploads/ {
    alias /app/uploads/;
    expires 30d;
    add_header Cache-Control "public, immutable";
    
    # Compressão
    gzip on;
    gzip_types image/jpeg image/png image/gif application/pdf;
    
    # Proteção
    limit_except GET {
        deny all;
    }
}
